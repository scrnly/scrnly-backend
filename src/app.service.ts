import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService {
  // Scaffolded by NestJS. Remove this comment when you add your own code.
  getHello(): string {
    return 'Hello World!';
  }
}
